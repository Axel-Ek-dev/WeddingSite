/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Leave basePath blank — set during deployment if needed for GitHub Pages
}

module.exports = nextConfig
